# Template only — fill in and `kubectl apply -f` a copy that is NOT committed
# to source control (or better: manage this via a secrets manager / External
# Secrets Operator / sealed-secrets in a real cluster).
apiVersion: v1
kind: Secret
metadata:
  name: complianceai-secrets
  namespace: complianceai
type: Opaque
stringData:
  OPENAI_API_KEY: ""
  AZURE_OPENAI_KEY: ""
  AZURE_OPENAI_ENDPOINT: ""
  AZURE_SEARCH_ENDPOINT: ""
  AZURE_SEARCH_KEY: ""
  PURVIEW_ACCOUNT_NAME: ""
  PURVIEW_TENANT_ID: ""
  PURVIEW_CLIENT_ID: ""
  PURVIEW_CLIENT_SECRET: ""
  JWT_SECRET: "change-me-in-production"
  OIDC_ISSUER: ""            # e.g. https://keycloak.example.com/realms/complianceai
  OIDC_AUDIENCE: ""
  POSTGRES_PASSWORD: "complianceai"
