apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: complianceai-ingress
  namespace: complianceai
  annotations:
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"   # keep SSE/WebSocket alive
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
spec:
  ingressClassName: nginx
  rules:
    - host: complianceai.example.com
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend: { service: { name: backend, port: { number: 8000 } } }
          - path: /ws
            pathType: Prefix
            backend: { service: { name: backend, port: { number: 8000 } } }
          - path: /
            pathType: Prefix
            backend: { service: { name: frontend, port: { number: 5173 } } }
